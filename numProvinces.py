class Solution():
	def dfs(self, start):
		if 
	def findCircleNum(self, isConnected):
		self.isConnected = isConnected
		self.output = []
		self.n = len(isConnected)
		self.visited = [k]
		if self.n == 0:
			return []
		self.dfs(0, )
		return None

q = Solution()
print(q.simplifyPath(inputs))