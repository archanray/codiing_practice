class TreeNode:
    def __init__(self, val, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def DFS(self, root):
        return None
    def sumNumbers(self, root):
        self.DFS(root, "")
        return None